import { test, expect } from "@playwright/test";

test.describe("$FEATURE_NAME", () => {
  test("$FLOW_NAME", async ({ page }) => {
    // Preconditions:
    // $PRECONDITIONS

    // Steps:
    // Use test.step per OpenSpec step for readable traces.
    // $STEPS

    // Acceptance Criteria:
    // $ASSERTIONS
  });
});
